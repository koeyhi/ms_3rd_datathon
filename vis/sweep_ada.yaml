method: bayes
metric:
  name: accuracy
  goal: maximize
parameters:
  n_estimators:
    distribution: q_uniform
    min: 200
    max: 400
  learning_rate:
    distribution: uniform
    min: 0.1
    max: 0.25
  algorithm:
    distribution: categorical
    values: ["SAMME.R"]

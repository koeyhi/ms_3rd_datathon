method: bayes
metric:
  name: accuracy
  goal: maximize
parameters:
  C:
    distribution: log_uniform
    min: 0.001
    max: 10
  penalty:
    values: ["l1", "l2"]
  solver:
    values: ["liblinear", "saga"]
  max_iter:
    distribution: q_uniform
    min: 100
    max: 2000